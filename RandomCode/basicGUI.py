from tkinter import *

#main
windows = Tk()
#tkinter works by starting a tcl/tk interpreter under the covers
#creating an instance of Tk initializes this interpreter and creates
#the root window

window.title("My coding")

#must be called for windows to be drawn and events to be processed
window.mainloop()